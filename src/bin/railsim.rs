use mallard::world::World;

/// railsim binary for testing instantiation of the various components
/// To be used as a template for later users.
fn main() {
    let temp_world = World;
    temp_world.temp("blah".to_owned());
}
